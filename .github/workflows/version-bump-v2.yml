# DISABLED: Version bump workflow - using manual versioning instead
name: "version-bump-ai-ml-disabled"

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled. Using manual versioning instead."
          echo "Version bump logic has been moved to individual workflows."
